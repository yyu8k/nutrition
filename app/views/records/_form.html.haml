.record-form
= form_for @record do |f|
  - if @record.errors.any?
    #error_explanation
      %h2= "#{pluralize(@record.errors.count, "error")} prohibited this record from being saved:"
      %ul
        - @record.errors.full_messages.each do |message|
          %li= message

  .record-form__field
    .record-form__field__date
      %h1 Date
      = f.date_select :date, {with_css_classes: true}
    .record-form__field__food
      %h1 Food
      = f.text_field :food_id, class: "records_foods", id: "food-search-field", value: ""
      #food-search-result
    .record-form__field__result
    #redords-foods
      -# - @record.foods.each do |food|
      -#   - if gohan.name != current_food.name
      -#     .redords-food.clearfix
      -#       %input{name: "record[food_ids][]", type: "hidden", value: food.id}/
      -#       %p.redords-food__name= food.name
      -#       %a.gohan-search-remove.redords-food__btn.redords-food__btn--remove.js-remove-btn 削除

  .record-form__actions
    = f.submit 'Save', class: "records_save"
